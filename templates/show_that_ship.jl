using Plots

# Construct and plot lines with two points
X = 
Y1 = 
Y2 =

# Plot
plot(X,Y1, label="", lw=, color=)
plot!(X,Y2, label="", lw=, color=) # plot! adds a new line to the existing plot
plot!([x_ship], [y_ship], marker=,color=, label="")

# Give plot limits, title, axes labels
plot!(xlims=, ylims=, title="", xlabel="", ylabel="")

